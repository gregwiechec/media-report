import React from 'react'
import ReactDOM from 'react-dom/client'
import MediaReport from './media-report'

ReactDOM.createRoot(document.getElementById("media-report-root") as HTMLElement).render(
  <React.StrictMode>
    <MediaReport />
  </React.StrictMode>,
)
